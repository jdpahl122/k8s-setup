# Vault initialization output — unseal keys + root token. NEVER commit.
.vault/

# kubeconfig / local creds
kubeconfig
*.kubeconfig

# Exported local CA cert (public, but environment-specific — regenerate per machine)
local-ca.crt
